name: Container Images

on:
  push:
    branches: [main]
    tags: ["v*"]

permissions:
  contents: read
  packages: write

jobs:
  test:
    if: github.ref_type == 'branch'
    uses: ./.github/workflows/test.yml

  build:
    if: github.ref_type == 'branch'
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image:
          - name: waypoint
            dockerfile: Dockerfile.waypoint
          - name: waypoint-monitor
            dockerfile: Dockerfile.waypoint-monitor
    steps:
      - uses: actions/checkout@v5

      - uses: docker/setup-buildx-action@v4

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v6
        id: meta
        with:
          images: ghcr.io/redoapp/${{ matrix.image.name }}
          tags: |
            type=ref,event=branch
            type=sha

      - uses: docker/build-push-action@v7
        with:
          context: .
          file: ${{ matrix.image.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image:
          - name: waypoint
          - name: waypoint-monitor
    steps:
      - uses: docker/setup-buildx-action@v4

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v6
        id: meta
        with:
          images: ghcr.io/redoapp/${{ matrix.image.name }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Wait for image to be available
        run: |
          IMAGE="ghcr.io/redoapp/${{ matrix.image.name }}:sha-${GITHUB_SHA::7}"
          for i in $(seq 1 30); do
            if docker buildx imagetools inspect "$IMAGE" > /dev/null 2>&1; then
              echo "Image $IMAGE found"
              exit 0
            fi
            echo "Image not found, waiting 30s... (attempt $i/30)"
            sleep 30
          done
          echo "Timed out waiting for $IMAGE"
          exit 1

      - name: Retag existing image with semver tags
        env:
          TAGS: ${{ steps.meta.outputs.tags }}
        run: |
          echo "$TAGS" | while read -r TAG; do
            [ -z "$TAG" ] && continue
            docker buildx imagetools create \
              --tag "$TAG" \
              ghcr.io/redoapp/${{ matrix.image.name }}:sha-${GITHUB_SHA::7}
          done
